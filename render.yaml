services:
  - type: web
    name: projectgen-token-server
    runtime: python
    buildCommand: pip install -r requirements-backend.txt
    startCommand: gunicorn -w 1 -b 0.0.0.0:$PORT src.token_server:app
    envVars:
      - key: LIVEKIT_API_KEY
        sync: false
      - key: LIVEKIT_API_SECRET
        sync: false
      - key: PYTHON_VERSION
        value: 3.12.0
